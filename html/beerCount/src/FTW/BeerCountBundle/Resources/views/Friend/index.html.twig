{% extends '::base.html.twig' %}

{% block body -%}
    
    <div class="page-header">
        <h1>Friend list</h1>
    </div>

    <div class="row">
        <div class="col">
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Nickname</th>
                <th>Favoritebeer</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
                {% for entity in entities %}
                    <tr>
                        <td><a href="{{ path('friend_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                        <td>{{ entity.name }}</td>
                        <td>{{ entity.email }}</td>
                        <td>{{ entity.nickname }}</td>
                        <td>{{ entity.favoriteBeer }}</td>
                        <td>
                        <ul>
                            <li>
                                <a href="{{ path('friend_show', { 'id': entity.id }) }}">show</a>
                            </li>
                            <li>
                                <a href="{{ path('friend_edit', { 'id': entity.id }) }}">edit</a>
                            </li>
                        </ul>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
          </table>
        </div>
      </div>

    <a role="button" class="btn btn-primary btn-lg" href="{{ path('friend_new') }}">Create a new entry</a>

{% endblock %}
